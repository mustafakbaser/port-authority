# Fixture only — no real credentials.
DATABASE_URL=postgresql://sample:sample@localhost:51992/sample
REDIS_URL=redis://redis.production.example.com:6379
CACHE_PORT=51993
INTERPOLATED_PORT=${BASE_PORT}
# A remote host pairs with this port, so it must not be treated as a local expectation.
QUEUE_HOST=queue.prod.example.com
QUEUE_PORT=51996
